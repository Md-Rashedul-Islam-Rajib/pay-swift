import React from 'react';

const Login = () => {
    
    const CashIn = () => {
        const {
          register,
          handleSubmit,
          formState: { errors },
        } = useForm();

    return (
        <div>
            
        </div>
    );
};

export default Login;